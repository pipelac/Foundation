# ChannelStories - Класс для работы с постами телеграм каналов

## Описание

`ChannelStories` - это класс для управления постами в телеграм каналах через Bot API. Он предоставляет удобный интерфейс для создания, редактирования, удаления и управления постами.

**ВАЖНО:** Telegram Bot API не поддерживает работу со Stories (истории) напрямую. Stories доступны только через Client API (MTProto). Этот класс работает с обычными постами канала, предоставляя функциональность, аналогичную работе со Stories.

## Основные возможности

- ✅ Отправка фото-постов в канал
- ✅ Отправка видео-постов в канал
- ✅ Редактирование подписей к постам
- ✅ Удаление постов из канала
- ✅ Закрепление/открепление постов
- ✅ Отслеживание активных постов
- ✅ Ротация постов (удаление старых, добавление новых)
- ✅ Пакетное удаление всех отслеживаемых постов

## Требования

- PHP 8.1+
- Telegram Bot с правами администратора в целевом канале
- Зависимости:
  - `TelegramAPI` - для работы с Telegram Bot API
  - `Logger` - для логирования операций (опционально)

## Установка

Класс находится в `src/TelegramBot/Core/ChannelStories.php` и подключается через автозагрузчик проекта.

## Использование

### Инициализация

```php
use App\Component\Logger;
use App\Component\Http;
use App\Component\TelegramBot\Core\TelegramAPI;
use App\Component\TelegramBot\Core\ChannelStories;

// Инициализация зависимостей
$logger = new Logger(['directory' => './logs']);
$http = new Http(['timeout' => 60], $logger);
$api = new TelegramAPI('YOUR_BOT_TOKEN', $http, $logger);

// Создание экземпляра ChannelStories
$stories = new ChannelStories($api, $logger);
```

### Отправка фото-поста

```php
$channelId = '@your_channel';
$photoPath = '/path/to/photo.jpg';

$message = $stories->sendPhotoStory($channelId, $photoPath, [
    'caption' => '📸 Новый пост в канале!',
    'parse_mode' => TelegramAPI::PARSE_MODE_HTML,
]);

echo "Пост отправлен! Message ID: {$message->messageId}\n";
```

### Отправка видео-поста

```php
$videoPath = '/path/to/video.mp4';

$message = $stories->sendVideoStory($channelId, $videoPath, [
    'caption' => '🎥 Новое видео!',
    'duration' => 30,
    'parse_mode' => TelegramAPI::PARSE_MODE_HTML,
]);
```

### Редактирование подписи

```php
$messageId = 123; // ID сообщения для редактирования

$stories->editStoryCaption(
    $channelId,
    $messageId,
    "✏️ Обновленная подпись",
    ['parse_mode' => TelegramAPI::PARSE_MODE_HTML]
);
```

### Закрепление поста

```php
// Закрепить пост (с уведомлением подписчиков)
$stories->pinStory($channelId, $messageId, false);

// Закрепить без уведомления
$stories->pinStory($channelId, $messageId, true);
```

### Открепление поста

```php
// Открепить конкретный пост
$stories->unpinStory($channelId, $messageId);

// Открепить все посты
$stories->unpinStory($channelId);
```

### Удаление поста

```php
$result = $stories->deleteStory($channelId, $messageId);

if ($result) {
    echo "Пост успешно удален\n";
}
```

### Ротация постов

Ротация позволяет автоматически удалять старые посты и добавлять новые, поддерживая заданное максимальное количество постов.

```php
// Подготовка новых постов
$newPosts = [
    [
        'type' => 'photo',
        'media' => '/path/to/photo1.jpg',
        'options' => [
            'caption' => 'Пост #1',
            'parse_mode' => TelegramAPI::PARSE_MODE_HTML,
        ],
    ],
    [
        'type' => 'video',
        'media' => '/path/to/video1.mp4',
        'options' => [
            'caption' => 'Пост #2',
            'duration' => 25,
        ],
    ],
];

// Выполнение ротации (максимум 5 постов)
$stats = $stories->rotateStories($channelId, 5, $newPosts);

echo "Удалено: {$stats['deleted']}, Добавлено: {$stats['added']}, Активных: {$stats['active']}\n";
```

### Удаление всех отслеживаемых постов

```php
$deletedCount = $stories->deleteAllStories($channelId);
echo "Удалено постов: {$deletedCount}\n";
```

### Работа с отслеживанием постов

```php
// Получить список отслеживаемых ID
$messageIds = $stories->getTrackedMessages();
echo "Отслеживается постов: " . count($messageIds) . "\n";

// Добавить сообщение в отслеживание
$stories->trackMessage(456);

// Удалить из отслеживания
$stories->untrackMessage(456);

// Очистить весь список отслеживания
$stories->clearTracking();
```

## API методы

### sendPhotoStory()

Отправляет фото-пост в канал.

**Параметры:**
- `string|int $chatId` - ID канала или @username
- `string $photo` - Путь к файлу, URL или file_id
- `array $options` - Дополнительные параметры (caption, parse_mode, etc.)

**Возвращает:** `Message`

### sendVideoStory()

Отправляет видео-пост в канал.

**Параметры:**
- `string|int $chatId` - ID канала или @username
- `string $video` - Путь к файлу, URL или file_id
- `array $options` - Дополнительные параметры (caption, duration, width, height, etc.)

**Возвращает:** `Message`

### editStoryCaption()

Редактирует подпись к посту.

**Параметры:**
- `string|int $chatId` - ID канала или @username
- `int $messageId` - ID сообщения
- `string $caption` - Новая подпись
- `array $options` - Дополнительные параметры (parse_mode, etc.)

**Возвращает:** `Message`

### deleteStory()

Удаляет пост из канала.

**Параметры:**
- `string|int $chatId` - ID канала или @username
- `int $messageId` - ID сообщения

**Возвращает:** `bool`

### pinStory()

Закрепляет пост в канале.

**Параметры:**
- `string|int $chatId` - ID канала или @username
- `int $messageId` - ID сообщения
- `bool $disableNotification` - Отключить уведомление (по умолчанию false)

**Возвращает:** `bool`

### unpinStory()

Открепляет пост в канале.

**Параметры:**
- `string|int $chatId` - ID канала или @username
- `int|null $messageId` - ID сообщения (null для открепления всех)

**Возвращает:** `bool`

### rotateStories()

Выполняет ротацию постов.

**Параметры:**
- `string|int $chatId` - ID канала или @username
- `int $maxStories` - Максимальное количество постов
- `array $newStories` - Массив новых постов для добавления

**Возвращает:** `array{deleted: int, added: int, active: int}`

### deleteAllStories()

Удаляет все отслеживаемые посты.

**Параметры:**
- `string|int $chatId` - ID канала или @username

**Возвращает:** `int` - Количество удаленных постов

### getTrackedMessages()

Получает список отслеживаемых ID сообщений.

**Возвращает:** `array<int>`

### trackMessage()

Добавляет ID сообщения в список отслеживаемых.

**Параметры:**
- `int $messageId` - ID сообщения

### untrackMessage()

Удаляет ID сообщения из списка отслеживаемых.

**Параметры:**
- `int $messageId` - ID сообщения

### clearTracking()

Очищает список отслеживаемых сообщений.

## Логирование

Класс автоматически логирует все операции, если передан экземпляр Logger:

- **INFO**: Успешные операции и важные события
- **WARNING**: Частичные сбои при массовых операциях
- **ERROR**: Критические ошибки

Примеры логов:

```
INFO: Отправка фото-поста в канал {"chat_id":"@channel","has_caption":true}
INFO: Фото-пост успешно отправлен {"chat_id":"@channel","message_id":123}
ERROR: Ошибка отправки фото-поста {"chat_id":"@channel","error":"..."}
```

## Обработка ошибок

Все методы выбрасывают исключения при ошибках:

```php
use App\Component\TelegramBot\Exceptions\ApiException;
use App\Component\TelegramBot\Exceptions\ValidationException;

try {
    $message = $stories->sendPhotoStory($channelId, $photo, ['caption' => 'Test']);
} catch (ValidationException $e) {
    echo "Ошибка валидации: {$e->getMessage()}\n";
} catch (ApiException $e) {
    echo "Ошибка API: {$e->getMessage()}\n";
}
```

## Ограничения

1. **Права бота**: Бот должен быть администратором канала с правами на публикацию сообщений
2. **Stories API**: Класс работает с обычными постами, не с настоящими Stories (они доступны только через Client API)
3. **Размер файлов**: Ограничения Telegram Bot API:
   - Фото: до 10 MB
   - Видео: до 50 MB
4. **Rate limits**: Соблюдайте ограничения Telegram на количество запросов

## Тестирование

Для тестирования класса используйте `test_channel_stories.php`:

```bash
php test_channel_stories.php
```

Тест выполняет интерактивную проверку всех методов класса с отправкой уведомлений в Telegram.

## Лицензия

Этот класс является частью проекта и распространяется по той же лицензии, что и основной проект.

## Автор

Создано для проекта TelegramBot в рамках PHP 8.1+ toolkit.

## Changelog

### v1.0.0 (2025-11-02)
- Первый релиз
- Базовый функционал отправки, редактирования и удаления постов
- Закрепление/открепление постов
- Ротация постов
- Система отслеживания активных постов
