# Анализ создания класса OpenRouterResponseAnalysis.class.php

## Исходная ситуация

Имеется трейт `AIAnalysisTrait.php` (1291 строк) в проекте rss2tlg с полной функциональностью:
- Fallback механизм между моделями
- Retry механизм с экспоненциальной задержкой
- Кеширование промптов для Claude
- Извлечение JSON из ответов AI
- **Полноценная работа с БД** для хранения и анализа метрик OpenRouter
- Аналитика по периодам, моделям, кешированию
- Экспорт отчетов в JSON/CSV

## Задача

Создать новый **минималистичный** базовый класс `OpenRouterResponseAnalysis.class.php`, который:
1. Извлекает JSON из ответов OpenRouter API (с обработкой markdown блоков)
2. Написан по образу `OpenRouter.class.php` с возможностью логирования
3. **НЕ содержит** функции записи в БД или экспорта
4. **НЕ перегружен** избыточными методами

---

## Детальный анализ методов AIAnalysisTrait.php

### ✅ Методы, которые НУЖНЫ для базового класса

#### 1. **extractJSON(string $content): string** - КРИТИЧЕСКИ ВАЖЕН
```php
protected function extractJSON(string $content): string
```
**Назначение**: Извлечение JSON из ответа AI  
**Обработка**:
- Markdown блоки: ```json...``` или ```...```
- JSON объекты: {...}
- JSON массивы: [...]
- Текст с префиксом

**Почему нужен**: Это ОСНОВНАЯ функция нового класса!

#### 2. **prepareMessages()** - НУЖЕН
```php
protected function prepareMessages(string $systemPrompt, string $userPrompt, string $model): array
```
**Назначение**: Подготовка messages для AI запроса  
**Функциональность**:
- Обычный формат для большинства моделей
- Специальный формат с cache_control для Claude
- Поддержка кеширования промптов

**Почему нужен**: Базовая функция для правильной работы с разными моделями

#### 3. **prepareOptions()** - НУЖЕН
```php
protected function prepareOptions($modelConfig, ?array $extraOptions = null): array
```
**Назначение**: Подготовка опций для AI запроса  
**Функциональность**:
- Извлечение параметров из конфигурации модели
- Дефолтные значения
- Merge с дополнительными опциями

**Почему нужен**: Базовая функция для настройки запросов

#### 4. **validateAIConfig()** - НУЖЕН (упрощенный)
```php
protected function validateAIConfig(array $config): array
```
**Назначение**: Валидация конфигурации AI модулей  
**Функциональность**:
- Проверка наличия моделей
- Проверка наличия промпт файла
- Возврат валидированной конфигурации

**Почему нужен**: Базовая проверка перед запросом

---

### ❌ Методы, которые НЕ НУЖНЫ (избыточны для базового класса)

#### 1. **analyzeWithFallback()** - ❌ НЕТ
**Причина**: 
- Логика высокого уровня для pipeline
- Требует метрики, сложное логирование
- Fallback стратегия - не базовая функция
- Должен быть в отдельном классе или trait для конкретного проекта

#### 2. **callAI()** - ❌ НЕТ
**Причина**:
- Вызывает `OpenRouter.chatWithMessages()` (это уже есть в OpenRouter.class.php)
- Делает запись в БД через `recordDetailedMetrics()` (не нужно в базовом классе)
- Это обертка высокого уровня, не базовая функция

#### 3. **recordDetailedMetrics()** - ❌ НЕТ
**Причина**: 
- Запись в БД - не базовая функция
- Специфично для проекта rss2tlg
- Требует зависимость от MySQL

#### 4. **getDetailedMetrics()** - ❌ НЕТ
**Причина**: Чтение из БД - не базовая функция

#### 5. **getSummaryByPeriod()** - ❌ НЕТ
**Причина**: Аналитика по периодам - не базовая функция

#### 6. **getSummaryByModel()** - ❌ НЕТ
**Причина**: Аналитика по моделям - не базовая функция

#### 7. **getCacheAnalytics()** - ❌ НЕТ
**Причина**: Аналитика кеширования - не базовая функция

#### 8. **getDetailReport()** - ❌ НЕТ
**Причина**: Экспорт отчетов - не базовая функция

#### 9. **resolvePeriodRange()** - ❌ НЕТ
**Причина**: Вспомогательная функция для аналитики БД

#### 10. **resolveDateBounds()** - ❌ НЕТ
**Причина**: Вспомогательная функция для аналитики БД

#### 11. **createDateTime()** - ❌ НЕТ
**Причина**: Вспомогательная функция для аналитики БД

#### 12. **setMetricsDb()** - ❌ НЕТ
**Причина**: Установка БД - не нужна в базовом классе

---

## Итоговая структура OpenRouterResponseAnalysis.class.php

### Namespace
```php
namespace App\Component;
```

### Зависимости
- `Logger` (опционально для логирования)
- **БЕЗ** зависимости от `MySQL`
- **БЕЗ** зависимости от `OpenRouter` (это отдельная зона ответственности)

### Основные методы (5 методов)

#### 1. extractJSON()
```php
/**
 * Извлекает JSON из ответа AI (удаляет markdown блоки и префиксы)
 *
 * @param string $content Ответ от AI
 * @return string Очищенный JSON
 */
public static function extractJSON(string $content): string
```

#### 2. parseJSONResponse()
```php
/**
 * Парсит JSON ответ с обработкой ошибок
 *
 * @param string $content Ответ от AI (может содержать markdown)
 * @return array|null Распарсенный массив или null при ошибке
 */
public static function parseJSONResponse(string $content): ?array
```

#### 3. prepareMessages()
```php
/**
 * Подготавливает messages для AI запроса с поддержкой кеширования
 *
 * @param string $systemPrompt Системный промпт
 * @param string $userPrompt Пользовательский промпт
 * @param string $model Название модели (для определения поддержки кеширования)
 * @return array Массив messages
 */
public static function prepareMessages(string $systemPrompt, string $userPrompt, string $model): array
```

#### 4. prepareOptions()
```php
/**
 * Подготавливает опции для AI запроса
 *
 * @param array|string $modelConfig Конфигурация модели или строка с названием
 * @param array|null $extraOptions Дополнительные опции
 * @return array Готовые опции для запроса
 */
public static function prepareOptions($modelConfig, ?array $extraOptions = null): array
```

#### 5. validateAIConfig()
```php
/**
 * Валидирует конфигурацию AI модулей
 *
 * @param array $config Конфигурация для валидации
 * @return array Валидированная конфигурация
 * @throws OpenRouterValidationException
 */
public static function validateAIConfig(array $config): array
```

### Дополнительные вспомогательные методы (опционально, 3 метода)

#### 6. detectJSONInText()
```php
/**
 * Обнаруживает JSON в тексте (разные форматы)
 *
 * @param string $content Текст для анализа
 * @return array|null Найденный JSON или null
 */
public static function detectJSONInText(string $content): ?array
```

#### 7. cleanMarkdown()
```php
/**
 * Очищает markdown блоки из текста
 *
 * @param string $content Текст с markdown
 * @return string Очищенный текст
 */
public static function cleanMarkdown(string $content): string
```

#### 8. extractCodeBlock()
```php
/**
 * Извлекает код из markdown блока
 *
 * @param string $content Текст с code block
 * @param string $language Язык кода (json, php, etc.) или пустая строка для любого
 * @return string|null Извлеченный код или null
 */
public static function extractCodeBlock(string $content, string $language = ''): ?string
```

---

## Сравнительная таблица

| Метод | AIAnalysisTrait | Новый класс | Причина |
|-------|----------------|-------------|---------|
| extractJSON | ✅ | ✅ | Основная функция |
| parseJSONResponse | ❌ | ✅ | Новый метод (обертка) |
| prepareMessages | ✅ | ✅ | Базовая функция |
| prepareOptions | ✅ | ✅ | Базовая функция |
| validateAIConfig | ✅ | ✅ | Базовая валидация |
| analyzeWithFallback | ✅ | ❌ | Высокоуровневая логика |
| callAI | ✅ | ❌ | Обертка с БД |
| recordDetailedMetrics | ✅ | ❌ | Запись в БД |
| getDetailedMetrics | ✅ | ❌ | Чтение из БД |
| getSummaryByPeriod | ✅ | ❌ | Аналитика |
| getSummaryByModel | ✅ | ❌ | Аналитика |
| getCacheAnalytics | ✅ | ❌ | Аналитика |
| getDetailReport | ✅ | ❌ | Экспорт |
| resolvePeriodRange | ✅ | ❌ | Вспомогательная (БД) |
| resolveDateBounds | ✅ | ❌ | Вспомогательная (БД) |
| createDateTime | ✅ | ❌ | Вспомогательная (БД) |
| setMetricsDb | ✅ | ❌ | Установка БД |
| detectJSONInText | ❌ | ✅ | Новая утилита |
| cleanMarkdown | ❌ | ✅ | Новая утилита |
| extractCodeBlock | ❌ | ✅ | Новая утилита |

**Итого**: 
- AIAnalysisTrait: **16 методов** (все с БД/аналитикой)
- Новый класс: **8 методов** (только парсинг/подготовка, БЕЗ БД)
- Сокращение: **50%** (убрали все методы БД/аналитики)

---

## Архитектурные решения

### 1. Статические методы vs Экземпляр класса

**Рекомендация**: Сделать методы **статическими**

**Обоснование**:
- Методы не требуют состояния (stateless)
- Можно использовать без создания экземпляра
- Упрощает использование в других классах
- По аналогии с utility классами

**Исключение**: Если нужен Logger, то:
```php
private static ?Logger $logger = null;

public static function setLogger(?Logger $logger): void
{
    self::$logger = $logger;
}
```

### 2. Логирование

**Рекомендация**: Опциональное логирование через статический Logger

```php
private static function logDebug(string $message, array $context = []): void
{
    self::$logger?->debug($message, $context);
}
```

### 3. Exception handling

**Рекомендация**: Использовать существующие exceptions:
- `OpenRouterValidationException` - для ошибок валидации
- `OpenRouterException` - для общих ошибок

### 4. Namespace и расположение

```
src/BaseUtils/OpenRouterResponseAnalysis.class.php
namespace App\Component;
```

---

## Преимущества нового класса

### 1. Минималистичность
- **Только 8 методов** вместо 16
- Фокус на одной задаче: парсинг и подготовка данных
- Без избыточных зависимостей

### 2. Переиспользуемость
- Статические методы
- Можно использовать в любом проекте
- Не привязан к конкретной БД или pipeline

### 3. Поддерживаемость
- Простая структура
- Понятное назначение каждого метода
- Легко тестировать

### 4. Расширяемость
- Можно добавить поддержку других AI API (не только OpenRouter)
- Можно расширить методы парсинга
- Можно добавить новые форматы ответов

---

## Использование в проекте rss2tlg

После создания нового класса, `AIAnalysisTrait.php` будет использовать его:

```php
trait AIAnalysisTrait
{
    protected function callAI(...)
    {
        // ... существующий код ...
        
        // Используем новый класс для извлечения JSON
        $jsonContent = OpenRouterResponseAnalysis::extractJSON($content);
        
        // Парсим JSON
        $analysisData = OpenRouterResponseAnalysis::parseJSONResponse($content);
        
        // ... остальной код (запись в БД, метрики) ...
    }
    
    protected function prepareMessages(string $systemPrompt, string $userPrompt, string $model): array
    {
        // Делегируем базовому классу
        return OpenRouterResponseAnalysis::prepareMessages($systemPrompt, $userPrompt, $model);
    }
    
    // ... остальные методы (БД, аналитика) остаются в trait ...
}
```

---

## Примеры использования нового класса

### Пример 1: Извлечение JSON из ответа AI

```php
use App\Component\OpenRouterResponseAnalysis;

// Ответ от AI с markdown блоком
$response = "```json\n{\"status\": \"ok\", \"data\": [1, 2, 3]}\n```";

// Извлекаем чистый JSON
$json = OpenRouterResponseAnalysis::extractJSON($response);
// Результат: {"status": "ok", "data": [1, 2, 3]}

// Парсим в массив
$data = OpenRouterResponseAnalysis::parseJSONResponse($response);
// Результат: ['status' => 'ok', 'data' => [1, 2, 3]]
```

### Пример 2: Подготовка сообщений для Claude с кешированием

```php
$messages = OpenRouterResponseAnalysis::prepareMessages(
    systemPrompt: 'You are a helpful assistant',
    userPrompt: 'Summarize this text',
    model: 'anthropic/claude-3.5-sonnet'
);

// Результат для Claude (с cache_control):
// [
//   [
//     'role' => 'system',
//     'content' => [
//       [
//         'type' => 'text',
//         'text' => 'You are a helpful assistant',
//         'cache_control' => ['type' => 'ephemeral']
//       ]
//     ]
//   ],
//   [
//     'role' => 'user',
//     'content' => 'Summarize this text'
//   ]
// ]
```

### Пример 3: Подготовка опций с конфигурацией модели

```php
$modelConfig = [
    'model' => 'openai/gpt-4',
    'max_tokens' => 2000,
    'temperature' => 0.7,
    'top_p' => 0.9
];

$options = OpenRouterResponseAnalysis::prepareOptions($modelConfig);
// Результат:
// [
//   'response_format' => ['type' => 'json_object'],
//   'max_tokens' => 2000,
//   'temperature' => 0.7,
//   'top_p' => 0.9
// ]
```

---

## Выводы

### ✅ Что получаем

1. **Чистый минималистичный класс** для работы с ответами AI API
2. **Переиспользуемый** код для других проектов
3. **Без избыточных зависимостей** (БД, аналитика)
4. **Статические методы** для удобства использования
5. **Фокус на основной задаче**: парсинг и подготовка данных

### 📊 Статистика

- **Методов в AIAnalysisTrait**: 16 (из них 11 для БД/аналитики)
- **Методов в новом классе**: 8 (только парсинг/подготовка)
- **Сокращение**: 50%
- **Строк кода**: ~1291 → ~250 (примерно)
- **Зависимостей**: MySQL, OpenRouter → только Logger (опционально)

### 🎯 Рекомендации

1. **Создать класс** `OpenRouterResponseAnalysis.class.php` в `src/BaseUtils/`
2. **Использовать статические методы** для удобства
3. **Добавить опциональное логирование** через статический Logger
4. **Обновить AIAnalysisTrait** для использования нового класса
5. **Добавить unit-тесты** для нового класса
6. **Обновить документацию** с примерами использования

---

## Следующие шаги

1. ✅ Анализ завершен
2. 🔨 Создать `OpenRouterResponseAnalysis.class.php`
3. 🔄 Рефакторинг `AIAnalysisTrait.php` для использования нового класса
4. 📝 Написать unit-тесты
5. 📚 Обновить документацию
6. ✔️ Code review и тестирование
