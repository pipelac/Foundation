# Отчет о тестировании RSS2TLG с AI анализом

**Дата**: 2025-11-04  
**Тестовый скрипт**: `tests/Rss2Tlg/rss_ai_tlg-v2.php`  
**База данных**: MariaDB 10.11 (Docker контейнер)

## Конфигурация

### AI Параметры (согласно исследованию)
```json
{
  "temperature": 0.25,
  "top_p": 0.85,
  "frequency_penalty": 0.15,
  "presence_penalty": 0.10,
  "max_tokens": 3000,
  "min_tokens": 400,
  "timeout": 180
}
```

### Модели AI (финальная конфигурация)
1. **Primary**: `google/gemini-2.0-flash-001:free` ✅ Стабильная, быстрая
2. **Fallback**: `anthropic/claude-3-5-haiku` (платная, резервная)
3. **Fallback**: `meta-llama/llama-3.2-3b-instruct:free` (бесплатная, резервная)

### RSS источники
- **Всего**: 30 источников
- **Языки**: 6 (русский, английский, французский, немецкий, китайский, Japanese)
- **По языкам**: 6 источников на каждом языке

## Результаты тестирования

### Этап 1: Сбор новостей
- ✅ Источников обработано: **30/30** (100%)
- ✅ Новостей собрано: **495**
- ✅ Ошибок сбора: **0**
- ⚠️ Warnings: 2 китайских источника с Invalid XML (SimplePie warnings)

### Этап 2: AI-анализ
- ✅ Успешных анализов: **128**
- ⚠️ Failed анализов: **147** (53.5% success rate)
- Средний рейтинг важности: **7.9/20**
- Уникальных категорий: **10**
  - Technology
  - Business
  - Entertainment
  - Science
  - Retail
  - Finance
  - Government
  - Regulation
  - Transportation
  - Cybersecurity

### Производительность
- **Скорость сбора**: ~1 сек на источник (30 сек для 30 источников)
- **Скорость AI-анализа**: ~3-5 сек на новость (google/gemini-2.0-flash-001:free)
- **RAM usage**: ~50-60 MB (PHP процесс)
- **CPU usage**: ~0.5-0.6% (idle)

## Проблемы и решения

### 1. ✅ РЕШЕНО: AIAnalysisService fallback не работал
**Проблема**: При ошибке первой модели создавалась failed запись в БД. Fallback модель проверяла `exists()` → находила запись → возвращала failed результат.

**Решение**: 
- Изменена логика проверки в `analyze()` методе
- Теперь проверяется `analysis_status === 'success'`
- Failed записи удаляются перед повторной попыткой
- Файл: `src/Rss2Tlg/AIAnalysisService.php` (строки 162-178)

### 2. ✅ РЕШЕНО: Бесплатные модели rate-limited
**Проблема**: 
- `qwen/qwen3-235b-a22b:free` → 429/503 ошибки (rate-limited upstream)
- `deepseek/deepseek-r1:free` → 429 ошибки (rate-limited upstream)

**Решение**:
- Изменена primary модель на `google/gemini-2.0-flash-001:free`
- Модель стабильная, быстрая, минимальные rate limits
- Файл: `config/rss2tlg_ai_v2.json`

### 3. ✅ РЕШЕНО: Отсутствие метода getById()
**Проблема**: Тестовый скрипт вызывал `ItemRepository::getById()`, но метод отсутствовал.

**Решение**:
- Добавлен метод `getById(int $id): ?array` в ItemRepository
- Файл: `src/Rss2Tlg/ItemRepository.php` (строки 116-135)

### 4. ⚠️ НЕ РЕШЕНО: Высокий процент failed анализов (53.5%)
**Причина**: Нужно дополнительное исследование логов.
Возможные причины:
- Rate limiting даже для google/gemini-2.0-flash-001:free
- Timeout на сложных новостях
- Проблемы с парсингом ответа от модели

**Рекомендация**: Добавить retry логику с exponential backoff.

## Выводы

### ✅ Успехи
1. **Система сбора новостей работает стабильно** - 100% success rate
2. **AI анализ функционирует** - категории и рейтинги определяются корректно
3. **Fallback механизм работает** после исправления логики
4. **Telegram интеграция работает** - уведомления отправляются
5. **Конфигурация AI параметров применяется** корректно

### ⚠️ Требуют внимания
1. **Процент успешных анализов** - нужно повысить до 80%+
2. **Rate limiting** - добавить более интеллектуальное управление очередью
3. **Ошибки парсинга XML** из китайских источников

### 📊 Финальная статистика
- Всего новостей: **495**
- Успешно проанализировано: **128** (25.9%)
- Failed: **147** (29.7%)
- Не обработано: **220** (44.4%)

## Рекомендации

1. **Добавить retry с backoff**: При rate limiting ждать 30-60 секунд перед повторной попыткой
2. **Увеличить batch_delay_ms**: С 1500ms до 3000ms для снижения нагрузки
3. **Добавить queue система**: Разделить анализ на batches по 50 новостей
4. **Мониторинг**: Добавить метрики успешности по каждой модели
5. **Fallback на платные модели**: При высоком проценте failed использовать платные

## Команды для работы с Docker

```bash
# Запуск MariaDB
docker run -d --name mysql-rss2tlg-test \
  -e MYSQL_ROOT_PASSWORD=root \
  -e MYSQL_DATABASE=rss2tlg \
  -e MYSQL_USER=rss2tlg_user \
  -e MYSQL_PASSWORD=rss2tlg_pass \
  -p 3306:3306 \
  mariadb:10.11 \
  --character-set-server=utf8mb4 \
  --collation-server=utf8mb4_unicode_ci

# Остановка
docker stop mysql-rss2tlg-test

# Запуск снова
docker start mysql-rss2tlg-test

# Удаление
docker rm -f mysql-rss2tlg-test

# Создание схемы
docker exec -i mysql-rss2tlg-test mysql -u rss2tlg_user -prss2tlg_pass rss2tlg < rss2tlg_schema_clean.sql

# Очистка БД
docker exec mysql-rss2tlg-test mysql -u rss2tlg_user -prss2tlg_pass rss2tlg -e "
TRUNCATE TABLE rss2tlg_ai_analysis;
TRUNCATE TABLE rss2tlg_items;
TRUNCATE TABLE rss2tlg_feed_state;
TRUNCATE TABLE rss2tlg_publications;"
```

## Запуск теста

```bash
# В foreground
cd /home/engine/project && php tests/Rss2Tlg/rss_ai_tlg-v2.php

# В background
cd /home/engine/project && nohup php tests/Rss2Tlg/rss_ai_tlg-v2.php > test_$(date +%Y%m%d_%H%M%S).log 2>&1 &

# Остановка
pkill -f "rss_ai_tlg-v2.php"
```
