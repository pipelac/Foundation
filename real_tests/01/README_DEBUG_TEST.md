# 📋 ИНСТРУКЦИИ ПО ЗАПУСКУ РЕАЛЬНОГО ТЕСТА

## 🎯 НАЗНАЧЕНИЕ

Детальное тестирование RSS2TLG с реальными API ключами и полной цепочкой обработки.

## 📋 ТРЕБОВАНИЯ

### 1. Программное обеспечение
- PHP 8.1+
- Docker & Docker Compose
- MariaDB/MySQL клиент

### 2. Внешние сервисы
- **OpenRouter API ключ** (реальный)
- **Telegram Bot токен** (реальный)
- **Доступ к Telegram каналу** (как администратор)

## 🚀 ЗАПУСК ТЕСТА

### Шаг 1: Подготовка окружения

```bash
# Создать папку для тестов
mkdir -p real_tests/01
cd real_tests/01

# Запустить MariaDB контейнер
docker run -d --name mysql-rss2tlg-real-test \
  -e MYSQL_ROOT_PASSWORD=root \
  -e MYSQL_DATABASE=rss2tlg \
  -e MYSQL_USER=rss2tlg_user \
  -e MYSQL_PASSWORD=rss2tlg_pass \
  -p 3306:3306 \
  mariadb:10.11 \
  --character-set-server=utf8mb4 \
  --collation-server=utf8mb4_unicode_ci
```

### Шаг 2: Настройка конфигурации

1. **Редактировать `config.json`:**
   - `database.host` - IP адрес Docker контейнера
   - `openrouter.api_key` - реальный API ключ
   - `telegram_bot.token` - реальный токен бота
   - `telegram_bot.chat_id` - ваш chat_id
   - `telegram_channel.username` - ваш канал

2. **Проверить подключение к БД:**
```bash
# Узнать IP контейнера
docker inspect mysql-rss2tlg-real-test | grep IPAddress

# Проверить подключение
docker exec mysql-rss2tlg-real-test mysqladmin ping -u root -proot
```

### Шаг 3: Создание схемы БД

```bash
# Импортировать схему
docker exec -i mysql-rss2tlg-real-test mysql -u rss2tlg_user -prss2tlg_pass rss2tlg < schema.sql

# Импортировать данные о feeds
docker exec -i mysql-rss2tlg-real-test mysql -u rss2tlg_user -prss2tlg_pass rss2tlg < feeds_data.sql
```

### Шаг 4: Запуск теста

```bash
# Запустить тестовый скрипт
php real_test.php

# Или с таймаутом (рекомендуется)
timeout 300 php real_test.php
```

## 📊 ОЖИДАЕМЫЕ РЕЗУЛЬТАТЫ

### Успешный запуск
- Сбор новостей из 30 RSS источников
- AI-анализ через DeepSeek V3.1
- Публикация в Telegram бот и канал
- Создание отчетов в папке теста

### Время выполнения
- **Общее время:** ~3-5 минут
- **Сбор новостей:** ~20-30 секунд
- **AI-анализ:** ~2-3 минуты (10 статей)
- **Публикация:** ~10-15 секунд

## 📁 ФАЙЛЫ РЕЗУЛЬТАТОВ

После успешного выполнения будут созданы:

### Отчеты
- `DEBUG_TEST_REPORT.md` - полный детальный отчет
- `DEBUG_TEST_SUCCESS_SUMMARY.md` - краткая сводка
- `DEBUG_TEST_CORRECTIONS.md` - отчет об исправлениях
- `REAL_TEST_REPORT.json` - метрики в JSON

### Логи
- `logs/rss2tlg_real_test.log` - детальный лог выполнения
- `rss2tlg_real_test.log` - копия лога в папке теста

## 🔍 ПРОВЕРКА РЕЗУЛЬТАТОВ

### Telegram
```bash
# Проверить сообщения в боте
# Сообщения должны появиться в чате и канале
```

### База данных
```sql
-- Посмотреть новости
SELECT COUNT(*) FROM rss2tlg_items;

-- Посмотреть AI-анализ
SELECT * FROM rss2tlg_ai_analysis ORDER BY created_at DESC LIMIT 5;

-- Посмотреть публикации
SELECT * FROM rss2tlg_publications ORDER BY published_at DESC LIMIT 5;
```

### Логи
```bash
# Просмотреть лог выполнения
tail -f logs/rss2tlg_real_test.log

# Поиск ошибок
grep -i error logs/rss2tlg_real_test.log
```

## ⚠️ ВОЗМОЖНЫЕ ПРОБЛЕМЫ

### 1. Ошибка подключения к БД
**Решение:** Проверить IP адрес Docker контейнера

### 2. Ошибки RSS источников
**Решение:** Нормально, некоторые источники могут быть недоступны

### 3. Ошибки OpenRouter API
**Решение:** Проверить API ключ и баланс

### 4. Ошибки Telegram
**Решение:** Проверить токен и права администратора в канале

## 🎯 ДАЛЬНЕЙШИЕ ДЕЙСТВИЯ

После успешного теста:

1. **Проанализировать отчеты** для оптимизации
2. **Настроить production окружение**
3. **Создать cron job** для автоматического запуска
4. **Настроить мониторинг** и алерты

---

**Важно:** Тест использует реальные API ключи и может генерировать реальные затраты на OpenRouter API!