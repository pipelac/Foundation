# 🔬 DEBUG TEST: Детальная отладка цепочки RSS2TLG

## 📋 Описание

Детальный тест для отладки полной цепочки обработки одной иностранной новости от RSS ленты до публикации в Telegram.

## 🎯 Цель

Провести пошаговую отладку с детальным логированием:
1. Сбор новостей из RSS лент
2. Сохранение в БД (rss2tlg_items)
3. Извлечение полного текста (WebtExtractor)
4. AI-анализ и перевод (OpenRouter)
5. Сохранение результата (rss2tlg_ai_analysis)
6. Публикация в Telegram бот
7. Публикация в Telegram канал
8. Tracking публикаций (rss2tlg_publications)

## 📁 Файлы

- **Тест:** `rss_debug_single_news.php`
- **Конфиг:** `/config/rss2tlg_debug_test.json`
- **Отчеты:** 
  - `/DEBUG_TEST_REPORT.md` (полный детальный отчет)
  - `/DEBUG_TEST_SUCCESS_SUMMARY.md` (краткая сводка)
  - `/DEBUG_TEST_CORRECTIONS.md` (исправления)

## 🔧 Подготовка

### 1. Запуск MariaDB

```bash
docker run -d --name mysql-rss2tlg-test \
  -e MYSQL_ROOT_PASSWORD=root \
  -e MYSQL_DATABASE=rss2tlg \
  -e MYSQL_USER=rss2tlg_user \
  -e MYSQL_PASSWORD=rss2tlg_pass \
  -p 3306:3306 \
  mariadb:10.11 \
  --character-set-server=utf8mb4 \
  --collation-server=utf8mb4_unicode_ci
```

Проверка:
```bash
docker exec -i mysql-rss2tlg-test mariadb -uroot -proot -e "SELECT VERSION();"
```

### 2. Настройка конфигурации

Отредактируйте `/config/rss2tlg_debug_test.json`:

```json
{
  "telegram": {
    "bot_token": "YOUR_BOT_TOKEN",
    "chat_id": "YOUR_CHAT_ID",
    "channel_id": "@YourChannel"
  },
  "ai_analysis": {
    "api_key": "YOUR_OPENROUTER_API_KEY"
  }
}
```

### 3. Проверка зависимостей

```bash
composer install
```

## 🚀 Запуск теста

```bash
cd /home/engine/project
php tests/Rss2Tlg/rss_debug_single_news.php
```

С сохранением вывода:
```bash
php tests/Rss2Tlg/rss_debug_single_news.php 2>&1 | tee debug_output.log
```

## 📊 Что тестируется

### Источники RSS (30)

- **Русский (6):** РИА, ТАСС, Коммерсантъ, Habr, Lenta.ru, Российская Газета
- **English (6):** TechCrunch, The Verge, Wired, Ars Technica, Engadget, BBC
- **Français (6):** Le Monde, Numerama, Clubic, 01net, Next INpact, France 24
- **Deutsch (6):** Heise, Golem, PC-WELT, ComputerBase, CHIP, Süddeutsche
- **中文 (6):** WSJ中文, cnBeta, IT之家, 36氪, 爱范儿, 极客公园

Тест обрабатывает **только иностранные источники** (24 источника, исключая русский).

### Компоненты

1. ✅ **FetchRunner** - Сбор из RSS
2. ✅ **ItemRepository** - Сохранение в БД
3. ✅ **ContentExtractorService** - Извлечение полного текста
4. ✅ **AIAnalysisService** - AI-анализ через OpenRouter
5. ✅ **AIAnalysisRepository** - Сохранение результатов AI
6. ✅ **TelegramAPI** - Публикация в Telegram
7. ✅ **PublicationRepository** - Tracking публикаций

## 📈 Ожидаемые результаты

### Успешный тест
```
✅ Этап 1: Инициализация - OK
✅ Этап 2: Сбор ~60 новостей - OK
✅ Этап 3: Извлечение контента - OK (~4 сек)
✅ Этап 4: AI-анализ - OK (~17 сек)
✅ Этап 5: Проверка БД - OK
✅ Этап 6: Публикация в бот - OK
✅ Этап 7: Публикация в канал - OK
✅ Этап 8: Финальная проверка - OK

🎉 ПОЗДРАВЛЯЕМ! Тест завершен успешно.
```

### Метрики
- **Время сбора:** ~10 сек
- **Время извлечения:** ~4 сек
- **Время AI-анализа:** ~17 сек
- **Общее время:** ~33 сек
- **Токенов:** ~12,000
- **Стоимость:** ~$0.002

## 📱 Telegram уведомления

Тест отправляет **8 уведомлений** в бот:
1. Старт теста
2. Завершение сбора
3. Завершение извлечения
4. Завершение AI-анализа
5. Проверка БД
6. Публикация в бот
7. Публикация в канал
8. Финальный отчет

## 🔍 Логирование

### Файл лога
`/home/engine/project/logs/rss2tlg_debug_test.log`

### Проверка логов
```bash
# Последние 100 строк
tail -100 logs/rss2tlg_debug_test.log

# Поиск ошибок
grep -i error logs/rss2tlg_debug_test.log

# Поиск предупреждений
grep -i warning logs/rss2tlg_debug_test.log
```

## 🗄️ База данных

### Проверка таблиц
```sql
-- Проверка новости
SELECT * FROM rss2tlg_items WHERE id = 11;

-- Проверка AI-анализа
SELECT * FROM rss2tlg_ai_analysis WHERE item_id = 11;

-- Проверка публикаций
SELECT * FROM rss2tlg_publications WHERE item_id = 11;

-- Проверка состояния фида
SELECT * FROM rss2tlg_feed_state WHERE feed_id = 9;
```

### Очистка данных
```sql
-- Удалить тестовые данные
DELETE FROM rss2tlg_publications WHERE created_at >= '2025-11-04 22:00:00';
DELETE FROM rss2tlg_ai_analysis WHERE created_at >= '2025-11-04 22:00:00';
DELETE FROM rss2tlg_items WHERE created_at >= '2025-11-04 22:00:00';
```

## 🐛 Решение проблем

### MariaDB не запускается
```bash
# Остановить и удалить контейнер
docker stop mysql-rss2tlg-test
docker rm mysql-rss2tlg-test

# Запустить заново
docker run -d --name mysql-rss2tlg-test ...
```

### Telegram ошибки
- Проверить bot_token в конфиге
- Проверить права бота в канале (должен быть администратором)
- Проверить chat_id корректный

### OpenRouter ошибки
- Проверить API ключ
- Проверить баланс аккаунта
- Проверить доступность модели

### RSS ленты не скачиваются
- Проверить интернет соединение
- Некоторые источники могут блокировать по User-Agent
- Попробовать использовать прокси (в конфиге)

## 📊 Анализ результатов

### Где смотреть результаты

1. **Консольный вывод** - детальная информация о каждом шаге
2. **Лог-файл** - структурированное логирование всех операций
3. **Telegram бот** - уведомления о прогрессе
4. **Telegram канал** - опубликованная новость
5. **База данных** - все данные в таблицах

### Отчеты

После успешного теста создаются 3 отчета:

1. `DEBUG_TEST_REPORT.md` - полный детальный отчет со всеми данными
2. `DEBUG_TEST_SUCCESS_SUMMARY.md` - краткая сводка успеха
3. `DEBUG_TEST_CORRECTIONS.md` - отчет об исправлениях (если были)

## 💰 Стоимость теста

### Один запуск
- **OpenRouter:** ~$0.002
- **Telegram:** Бесплатно
- **RSS feeds:** Бесплатно

### Прогноз production
- **100 новостей/день:** ~$0.19/день = $5.70/месяц
- **365 дней:** ~$69.35/год

## ⚠️ Важные замечания

1. **Реальная новость будет опубликована** в Telegram канале
2. **Реальный AI запрос** будет выполнен (учитывается в балансе)
3. **Данные сохраняются в БД** (можно удалить после теста)
4. **Rate limiting:** Соблюдайте 200ms задержку между AI запросами

## 🎓 Использование в production

После успешного теста система готова к production. Для автоматизации:

```bash
# Создать cron job
*/5 * * * * cd /path/to/project && php bin/rss2tlg_daemon.php >> logs/daemon.log 2>&1
```

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи
2. Проверьте конфигурацию
3. Проверьте доступность внешних сервисов (Telegram, OpenRouter)
4. Создайте issue с приложением лог-файла

---

**Версия теста:** 1.0  
**Дата создания:** 2025-11-04  
**Статус:** ✅ Протестировано и работает
