# 🚀 Как запустить E2E тесты RSS2TLG

## Быстрый старт

### 1. Запуск MariaDB

```bash
# Остановить старый контейнер (если есть)
docker stop rss2tlg_mariadb && docker rm rss2tlg_mariadb

# Запустить новый контейнер MariaDB 11.3.2
docker run -d \
  --name rss2tlg_mariadb \
  -e MYSQL_ROOT_PASSWORD=testpass123 \
  -e MYSQL_DATABASE=rss2tlg_test \
  -e MYSQL_USER=rss2tlg_user \
  -e MYSQL_PASSWORD=testpass123 \
  -p 3307:3306 \
  mariadb:11.3.2 \
  --character-set-server=utf8mb4 \
  --collation-server=utf8mb4_unicode_ci

# Подождать 30 секунд для готовности
sleep 30

# Проверить готовность
docker exec rss2tlg_mariadb mariadb-admin ping -h localhost -u root -ptestpass123
```

### 2. Проверка конфигурации

Убедитесь, что `Config/rss2tlg_e2e_test.json` содержит:

```json
{
  "database": {
    "host": "127.0.0.1",
    "port": 3307,
    "username": "rss2tlg_user",
    "password": "testpass123"
  },
  "telegram": {
    "bot_token": "ваш_токен",
    "chat_id": "ваш_chat_id",
    "channel_id": "@ваш_канал"
  },
  "openrouter": {
    "api_key": "ваш_ключ_api"
  }
}
```

### 3. Запуск тестов

#### Полный Failover тест (рекомендуется)
```bash
cd /home/engine/project
php tests_rss2tlg_e2e_FAILOVER_TEST.php
```

**Что проверяет:**
- ✅ Опрос 5 RSS (все новости)
- ✅ Unicode Fix
- ✅ AI анализ (5 новостей)
- ✅ **AI Failover между моделями** 🎯
- ✅ Публикация в Telegram
- ✅ Дампы таблиц

**Длительность:** ~2-3 минуты

#### Быстрый тест (без AI)
```bash
php tests_rss2tlg_e2e_quick.php
```

**Длительность:** ~5 секунд

#### Полный тест (с AI, без Failover)
```bash
php tests_rss2tlg_e2e_FULL.php
```

**Длительность:** ~1-2 минуты

---

## 📊 Результаты

### После выполнения теста

**Отчеты:** `tests/E2E_FAILOVER_TEST_REPORT_*.md`  
**Дампы:** `tests/sql/rss2tlg_*_failover_*.csv`  
**Логи:** `/tmp/rss2tlg_failover_test.log`

### Проверка результатов

```bash
# Просмотр последнего отчета
ls -lt tests/E2E_*.md | head -1 | awk '{print $NF}' | xargs cat

# Проверка дампов
ls -lh tests/sql/*failover*.csv

# Проверка Unicode в дампах
grep -o '\\u[0-9a-fA-F]\{4\}' tests/sql/rss2tlg_items_failover_*.csv
# Должно быть пусто (нет escape-последовательностей)
```

### Проверка Telegram уведомлений

Во время теста вы получите уведомления:
1. 🚀 Старт теста
2. 🗑️ Очистка БД
3. 📡 Опрос RSS
4. 💾 Сохранение
5. 🤖 AI анализ
6. 📱 Публикация
7. 🏁 Финальные результаты

---

## 🔍 Устранение проблем

### MariaDB не запускается

```bash
# Проверить Docker
docker ps -a | grep rss2tlg_mariadb

# Проверить логи
docker logs rss2tlg_mariadb

# Перезапустить
docker restart rss2tlg_mariadb
```

### Ошибка подключения к БД

```bash
# Проверить порт
docker port rss2tlg_mariadb

# Проверить подключение
docker exec rss2tlg_mariadb mariadb -u rss2tlg_user -ptestpass123 rss2tlg_test -e "SELECT 1"
```

### AI анализ не работает

**Проверить API ключ:**
- OpenRouter: https://openrouter.ai/settings/keys
- Проверить баланс

**Проверить модели:**
```bash
# В Config/rss2tlg_e2e_test.json
"models": [
  "deepseek/deepseek-r1:free",      // Может быть недоступна
  "qwen/qwen3-235b-a22b:free",      // Может быть недоступна
  "deepseek/deepseek-v3.2-exp"      // Надежная платная
]
```

### Telegram не публикует

**Проверить:**
1. Bot token корректный
2. Bot добавлен в канал как администратор
3. Chat ID правильный

```bash
# Проверка bot token
curl https://api.telegram.org/bot<TOKEN>/getMe

# Проверка прав в канале
curl https://api.telegram.org/bot<TOKEN>/getChatMember?chat_id=@channel&user_id=<bot_id>
```

---

## 📝 Очистка после тестов

```bash
# Остановить и удалить контейнер
docker stop rss2tlg_mariadb
docker rm rss2tlg_mariadb

# Удалить кеш
rm -rf /tmp/rss2tlg_failover_cache

# Удалить логи
rm -f /tmp/rss2tlg_failover_test.log
```

---

## 🎯 Ожидаемые результаты

### ✅ Успешный тест:

```
╔═══════════════════════════════════════════════════════════╗
║              РЕЗУЛЬТАТЫ FAILOVER ТЕСТИРОВАНИЯ             ║
╚═══════════════════════════════════════════════════════════╝

⏱️  Длительность: ~160 сек
📡 RSS источников: 5 / 5
📰 Новостей получено: ~300+
💾 Новостей сохранено: ~300+
🤖 AI анализов: 5 успешных, 0 ошибок
🔄 AI Failover: 0-5 событий
📱 Публикаций: 3
❌ Ошибок: 0

✅✅✅ ТЕСТ PASSED! Все проверки успешны! ✅✅✅
🎯 AI Failover механизм ПРОВЕРЕН и работает!
```

### Файлы после теста:

```
tests/
├── E2E_FAILOVER_TEST_REPORT_*.md
├── E2E_FAILOVER_FINAL_SUMMARY.md
└── sql/
    ├── rss2tlg_items_failover_*.csv          (~316 записей)
    ├── rss2tlg_ai_analysis_failover_*.csv    (~5 записей)
    ├── rss2tlg_publications_failover_*.csv   (~6 записей)
    └── rss2tlg_feed_state_failover_*.csv     (~5 записей)
```

---

## 💡 Советы

1. **Первый запуск:**
   - Таблицы создаются автоматически
   - Процесс может занять ~3 минуты

2. **Повторный запуск:**
   - Таблицы очищаются автоматически
   - Можно запускать сразу

3. **AI Failover:**
   - Бесплатные модели часто недоступны (это нормально!)
   - Система автоматически переключится на платные
   - Это демонстрирует работу failover

4. **Стоимость:**
   - Бесплатные модели: $0
   - Платные модели: ~$0.01-0.05 за 5 анализов
   - Мониторьте баланс: https://openrouter.ai/credits

5. **Производительность:**
   - RSS опрос: ~30 сек
   - AI анализ: ~20-30 сек/новость
   - Публикация: ~1 сек/новость

---

*Последнее обновление: 2025-11-07*
