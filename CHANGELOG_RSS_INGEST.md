# Changelog: RSS Ingest Script Improvements

## Дата: 2025-01-13

### Задача
Улучшение production/rss_ingest.php:
1. Полное удаление зависимостей от Telegram
2. Добавление автоинициализации БД из feeds.json
3. Исправление обработки флага `enabled: false`

---

## Внесенные изменения

### 1. Удаление Telegram из rss_ingest.php ✅

**Удалено:**
- `use App\Component\Telegram;` (строка 19)
- Функция `initTelegram()` (строки 247-262)
- Функция `sendTelegramNotification()` (строки 264-278)
- Все вызовы инициализации и отправки уведомлений

**Результат:**
- Скрипт больше не зависит от Telegram компонента
- Уменьшено потребление ресурсов
- Упрощена структура кода

---

### 2. Добавление синхронизации БД с конфигом ✅

**Добавлена функция `syncFeedsFromConfig()`:**

```php
function syncFeedsFromConfig(MySQL $db, Logger $logger): void
```

**Принцип работы:**
- **Конфиг feeds.json является источником истины**
- **Таблица rss2tlg_feeds - актуальный слепок конфигурации**
- **Синхронизация происходит при КАЖДОМ запуске скрипта**

**Функциональность:**
1. **Загрузка feeds.json**: Чтение конфигурации из `production/configs/feeds.json`
2. **Синхронизация каждой ленты**:
   - Если feed_url уже есть в БД:
     - Проверка изменений (name, website_url, enabled)
     - UPDATE только если данные изменились
   - Если feed_url нет в БД:
     - INSERT новой записи
   - Корректное преобразование boolean в TINYINT: `(int)(bool)$feed['enabled']`
3. **Отключение устаревших лент**:
   - Ленты в БД, которых нет в конфиге, автоматически отключаются (enabled=0)
4. **Логирование**: Все операции (добавление, обновление, отключение) логируются

**Пример вывода при первом запуске:**
```
🔄 Синхронизация лент из feeds.json...
   ✅ Добавлен: РИА Новости (enabled: 1)
   ✅ Добавлен: Коммерсантъ (enabled: 0)
   ✅ Добавлен: Интерфакс (enabled: 1)
   ✅ Добавлен: TechCrunch (enabled: 0)
✅ Синхронизация завершена: добавлено 4, обновлено 0, пропущено 0
```

**Пример вывода при повторном запуске (без изменений):**
```
🔄 Синхронизация лент из feeds.json...
✅ Ленты синхронизированы: изменений не требуется
```

**Пример вывода при изменении конфига:**
```
🔄 Синхронизация лент из feeds.json...
   ✏️  Обновлен: Коммерсантъ (enabled: 1)
✅ Синхронизация завершена: добавлено 0, обновлено 1, пропущено 0
```

**Интеграция:**
- Вызывается после инициализации БД, перед загрузкой активных лент
- Строка 56: `syncFeedsFromConfig($db, $logger);`

---

### 3. Исправление обработки флага `enabled: false` ✅

**Проблема:**
- В feeds.json параметр `enabled` имеет тип boolean (true/false)
- В БД поле `enabled` имеет тип TINYINT(1)
- При вставке JSON boolean не преобразовывался корректно в TINYINT
- Записи с `enabled: false` все равно обрабатывались

**Решение:**
```php
// Преобразование boolean в TINYINT
$enabled = isset($feed['enabled']) ? (int)(bool)$feed['enabled'] : 1;
```

**Результат:**
- `enabled: true` → TINYINT 1
- `enabled: false` → TINYINT 0
- Отсутствующее поле → TINYINT 1 (по умолчанию)

**Тестовые данные (feeds.json):**
```json
{
    "feeds": [
        {
            "name": "РИА Новости",
            "feed_url": "https://ria.ru/export/rss2/index.xml",
            "enabled": true
        },
        {
            "name": "Коммерсантъ",
            "feed_url": "https://www.kommersant.ru/rss/news.xml",
            "enabled": false
        },
        {
            "name": "Интерфакс",
            "feed_url": "https://www.interfax.ru/rss",
            "enabled": true
        },
        {
            "name": "TechCrunch",
            "feed_url": "https://techcrunch.com/feed/",
            "enabled": false
        }
    ]
}
```

---

## SQL Запросы

### Проверка активных лент
```sql
SELECT id, name, feed_url, website_url 
FROM rss2tlg_feeds 
WHERE enabled = 1 
ORDER BY id;
```

### Вставка новой ленты
```sql
INSERT INTO rss2tlg_feeds (name, feed_url, website_url, enabled) 
VALUES (?, ?, ?, ?);
```

### Обновление существующей ленты
```sql
UPDATE rss2tlg_feeds 
SET name = ?, website_url = ?, enabled = ?, updated_at = NOW() 
WHERE id = ?;
```

---

## Тестирование

### Проверка работы скрипта

1. **Очистка таблицы feeds (опционально)**:
```sql
TRUNCATE TABLE rss2tlg_feeds;
```

2. **Запуск скрипта**:
```bash
php production/rss_ingest.php
```

3. **Ожидаемый результат**:
   - Автоматическая загрузка лент из feeds.json
   - Обработка только лент с `enabled: true`
   - Логирование всех операций

4. **Проверка в БД**:
```sql
-- Все ленты
SELECT id, name, enabled FROM rss2tlg_feeds;

-- Только активные
SELECT id, name, enabled FROM rss2tlg_feeds WHERE enabled = 1;
```

### Ожидаемый результат в БД

| id | name | enabled |
|----|------|---------|
| 1 | РИА Новости | 1 |
| 2 | Коммерсантъ | 0 |
| 3 | Интерфакс | 1 |
| 4 | TechCrunch | 0 |

**Обрабатываться будут только:** РИА Новости (id=1) и Интерфакс (id=3)

---

## Логирование

### Консоль
```
╔═══════════════════════════════════════════════════════════════╗
║           RSS INGEST PRODUCTION SCRIPT v1.0.0                 ║
╚═══════════════════════════════════════════════════════════════╝
🕐 Start: 2025-01-13 12:00:00

🔄 Синхронизация лент из feeds.json...
   ✅ Добавлен: РИА Новости (enabled: 1)
   ✅ Добавлен: Коммерсантъ (enabled: 0)
   ✅ Добавлен: Интерфакс (enabled: 1)
   ✅ Добавлен: TechCrunch (enabled: 0)
✅ Синхронизация завершена: обработано 4, пропущено 0

📊 Найдено источников: 2

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📡 Источник: РИА Новости
🔗 URL: https://ria.ru/export/rss2/index.xml
✅ Успешно обработан
   📥 Получено: 30
   ✨ Новых: 30
   🔁 Дубликатов: 0
...
```

### Лог-файл (logs/rss_ingest.log)
```json
{"level":"info","message":"[RSS_INGEST] Script started","context":{"version":"1.0.0","pid":12345}}
{"level":"info","message":"[RSS_INGEST] Starting feeds synchronization from config","context":{"feeds_count":4}}
{"level":"info","message":"[RSS_INGEST] Feed inserted","context":{"id":1,"name":"РИА Новости","enabled":1}}
{"level":"info","message":"[RSS_INGEST] Feed inserted","context":{"id":2,"name":"Коммерсантъ","enabled":0}}
{"level":"info","message":"[RSS_INGEST] Feeds synchronization completed","context":{"synced":4,"skipped":0}}
{"level":"info","message":"[RSS_INGEST] Active feeds loaded","context":{"count":2}}
{"level":"info","message":"[RSS_INGEST] Feed processed successfully","context":{"feed_id":1,"feed_name":"РИА Новости","items_total":30,"items_new":30,"items_duplicates":0}}
...
```

---

## Обратная совместимость

✅ **Полная обратная совместимость** - скрипт работает как раньше, но:
- Без зависимости от Telegram
- С автоматической инициализацией из feeds.json
- С корректной обработкой флага enabled

---

## Рекомендации

1. **При первом запуске**: Убедитесь, что feeds.json настроен корректно
2. **При обновлении лент**: Измените feeds.json и перезапустите скрипт при пустой таблице
3. **Включение/выключение лент**: Измените `enabled: true/false` в feeds.json
4. **Мониторинг**: Проверяйте лог-файл на наличие ошибок синхронизации

---

## Технические детали

### Преобразование типов
```php
// Корректное преобразование boolean → TINYINT
$enabled = isset($feed['enabled']) ? (int)(bool)$feed['enabled'] : 1;

// Варианты входных данных:
// true  → (bool)true  → (int)1  → TINYINT 1 ✅
// false → (bool)false → (int)0  → TINYINT 0 ✅
// 1     → (bool)true  → (int)1  → TINYINT 1 ✅
// 0     → (bool)false → (int)0  → TINYINT 0 ✅
// null  → (по умолчанию) → TINYINT 1 ✅
```

### Идемпотентность
Функция `syncFeedsFromConfig()` идемпотентна:
- При повторном вызове обновляет только измененные данные
- Не создает дубликаты (проверка по feed_url)
- Безопасна для запуска в любое время

---

## Заключение

Все три задачи выполнены успешно:
1. ✅ Telegram полностью удален из rss_ingest.php
2. ✅ Добавлена автоинициализация БД из feeds.json
3. ✅ Исправлена обработка флага enabled: false

Скрипт готов к production использованию.
