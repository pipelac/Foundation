# .gitignore - файл для указания игнорируемых Git'ом файлов и директорий
# Эти файлы не будут включены в репозиторий и не будут отслеживаться системой контроля версий

# Директория vendor/ - содержит зависимости, установленные через Composer
# Не включается в репозиторий, т.к. может быть восстановлена командой composer install
/vendor/

# Лог-файлы приложения
# Содержат отладочную информацию и не должны храниться в репозитории
/logs/*.log

# Ротированные лог-файлы (с суффиксами типа .log.1, .log.2 и т.д.)
# Создаются при ротации логов для сохранения истории
/logs/*.log.*

# composer.lock - файл с зафиксированными версиями зависимостей
# Обычно фиксируют в репозитории приложений, чтобы гарантировать одинаковые версии пакетов
# В данном примере игнорируется, чтобы упростить обновление зависимостей при работе с шаблоном
/composer.lock

# Директория .idea/ - настройки IDE PhpStorm/IntelliJ IDEA
# Содержит персональные настройки среды разработки
/.idea/

# Кэш результатов тестирования PHPUnit
# Ускоряет повторный запуск тестов, но не нужен в репозитории
.phpunit.result.cache
