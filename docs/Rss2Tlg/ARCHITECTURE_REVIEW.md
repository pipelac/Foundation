# 🔍 АРХИТЕКТУРНЫЙ АНАЛИЗ AI PIPELINE

**Дата:** 2025-11-08  
**Статус:** ⚠️ ТРЕБУЮТСЯ ИЗМЕНЕНИЯ В ТЗ

---

## ❌ КРИТИЧЕСКИЕ ПРОБЛЕМЫ В ТЗ

### 1. **ОШИБКА: "Все данные в одной таблице"**

#### Что написано в ТЗ:
- "Все созданные МОДУЛЕМ дедупликации данные должны располагаться в **этой же таблице**"
- "Все созданные МОДУЛЕМ перевода данные должны располагаться в **этой же таблице**"  
- "Все созданные МОДУЛЕМ иллюстраций данные должны располагаться в **этой же таблице**"

#### ❌ Почему это НЕПРАВИЛЬНО:

1. **Нарушение нормализации БД** - дублирование данных
2. **Невозможность параллельной обработки** - одна новость может переводиться на несколько языков
3. **Проблемы с версионированием** - нельзя отследить изменения в каждом этапе
4. **Сложность отладки** - все в одной куче
5. **Неэффективные индексы** - огромная таблица с разнородными данными

#### ✅ ПРАВИЛЬНАЯ АРХИТЕКТУРА (УЖЕ РЕАЛИЗОВАНА):

```
rss2tlg_items               [Сырые данные из RSS]
    ↓
rss2tlg_summarization      [Этап 1: Суммаризация]
    ↓
rss2tlg_deduplication      [Этап 2: Дедупликация]
    ↓
rss2tlg_translation        [Этап 3: Перевод (опционально)]
    ↓
rss2tlg_illustration       [Этап 4: Иллюстрации (опционально)]
    ↓
rss2tlg_publications       [Журнал публикаций]
```

**Связь:** через `item_id` + `feed_id` (Foreign Keys)

**Преимущества:**
- ✅ Модульность - каждый этап независим
- ✅ Масштабируемость - легко добавить новые этапы
- ✅ Гибкость - можно включить/выключить любой модуль
- ✅ Производительность - оптимальные индексы для каждого этапа
- ✅ Отказоустойчивость - сбой одного модуля не влияет на другие

---

### 2. **ОШИБКА: Дублирование данных в финальной таблице**

#### Что написано в ТЗ:
> По итогам работы всех AI классов в таблице должны присутствовать записи:
> - Заголовок, Полный текст, Массив медиа
> - Язык, Категории, Важность, Качество перевода
> - Метрики OpenRouter, Название модели
> - Теги дедупликации, Флаги...
> - И т.д. (20+ полей)

#### ❌ Почему это НЕПРАВИЛЬНО:

1. **Дублирование данных** - все уже есть в таблицах pipeline
2. **Рассинхронизация** - данные могут устареть
3. **Сложность обновления** - надо обновлять в N местах
4. **Увеличение объема БД** - в 3-5 раз
5. **Нарушение DRY** (Don't Repeat Yourself)

#### ✅ ПРАВИЛЬНОЕ РЕШЕНИЕ:

**Используем VIEW** `v_rss2tlg_full_pipeline` - виртуальная таблица, которая объединяет данные из всех этапов:

```sql
CREATE OR REPLACE VIEW v_rss2tlg_full_pipeline AS
SELECT 
    -- Оригинальная новость
    i.id, i.feed_id, i.title, i.link,
    
    -- Суммаризация
    s.headline, s.summary, s.category_primary, 
    s.importance_rating, s.article_language,
    
    -- Дедупликация
    d.is_duplicate, d.can_be_published, d.similarity_score,
    
    -- Перевод
    t.translated_headline, t.translated_summary, 
    t.quality_score,
    
    -- Иллюстрация
    il.image_path, il.image_url,
    
    -- Публикация
    p.message_id, p.published_at
    
FROM rss2tlg_items i
LEFT JOIN rss2tlg_summarization s ON i.id = s.item_id
LEFT JOIN rss2tlg_deduplication d ON i.id = d.item_id
LEFT JOIN rss2tlg_translation t ON i.id = t.item_id
LEFT JOIN rss2tlg_illustration il ON i.id = il.item_id
LEFT JOIN rss2tlg_publications p ON i.id = p.item_id;
```

**Преимущества VIEW:**
- ✅ Всегда актуальные данные
- ✅ Нет дублирования
- ✅ Один запрос для получения полной информации
- ✅ Не занимает место на диске

---

### 3. **ОШИБКА: Перегрузка таблицы публикаций**

#### Что написано в ТЗ:
> В таблицу публикаций добавить много полей контента

#### ❌ Почему это НЕПРАВИЛЬНО:

Таблица `rss2tlg_publications` - это **ЖУРНАЛ ФАКТА** публикации, а не хранилище контента.

#### ✅ ПРАВИЛЬНОЕ РЕШЕНИЕ (УЖЕ РЕАЛИЗОВАНО):

Минимальный набор полей для фиксации **что именно было опубликовано**:

```sql
ALTER TABLE rss2tlg_publications 
    ADD COLUMN published_headline VARCHAR(500),      -- Заголовок
    ADD COLUMN published_text TEXT,                  -- Текст
    ADD COLUMN published_language VARCHAR(10),       -- Язык
    ADD COLUMN published_media JSON,                 -- Медиа
    ADD COLUMN published_categories JSON,            -- Категории
    ADD COLUMN importance_rating TINYINT UNSIGNED;   -- Важность
```

**Зачем это нужно?**
- Фиксируем snapshot того, что было опубликовано
- Даже если оригинальные данные изменятся, мы знаем что отправили
- Можно переопубликовать точную копию

---

## ✅ ПРАВИЛЬНАЯ АРХИТЕКТУРА (РЕАЛИЗОВАНА)

### Схема БД

```
┌─────────────────────┐
│  rss2tlg_items      │  ← Сырые данные из RSS
│  - id (PK)          │
│  - feed_id          │
│  - title            │
│  - content          │
│  - link             │
└──────────┬──────────┘
           │
           ↓
┌─────────────────────┐
│ rss2tlg_summarization│  ← ЭТАП 1: Суммаризация
│  - item_id (FK)     │
│  - headline         │
│  - summary          │
│  - category_primary │
│  - importance_rating│
│  - article_language │
│  - keywords         │
│  - dedup_data       │
└──────────┬──────────┘
           │
           ↓
┌─────────────────────┐
│ rss2tlg_deduplication│  ← ЭТАП 2: Дедупликация
│  - item_id (FK)     │
│  - is_duplicate     │
│  - can_be_published │
│  - similarity_score │
│  - duplicate_of_id  │
└──────────┬──────────┘
           │
           ↓
┌─────────────────────┐
│ rss2tlg_translation │  ← ЭТАП 3: Перевод (опционально)
│  - item_id (FK)     │
│  - source_language  │
│  - target_language  │
│  - translated_text  │
│  - quality_score    │
└──────────┬──────────┘
           │
           ↓
┌─────────────────────┐
│ rss2tlg_illustration│  ← ЭТАП 4: Иллюстрации (опционально)
│  - item_id (FK)     │
│  - image_path       │
│  - image_url        │
│  - prompt_used      │
└──────────┬──────────┘
           │
           ↓
┌─────────────────────┐
│ rss2tlg_publications│  ← Журнал публикаций
│  - item_id (FK)     │
│  - message_id       │
│  - published_at     │
│  - published_text   │
└─────────────────────┘
```

### Ключевые принципы:

1. **Модульность** - каждый этап в отдельной таблице
2. **Связность** - через `item_id` и `feed_id`
3. **Независимость** - можно включить/выключить любой модуль
4. **Масштабируемость** - легко добавить новые этапы
5. **Производительность** - индексы оптимизированы для каждого этапа

---

## 🎯 РЕКОМЕНДАЦИИ

### ✅ ЧТО ОСТАВЛЯЕМ КАК ЕСТЬ:

1. **Схему таблиц** - уже правильная (`ai_pipeline_schema.sql`)
2. **VIEW** для объединения данных
3. **Модульную архитектуру** классов

### ⚠️ ЧТО НУЖНО ДОРАБОТАТЬ:

1. **OutputSchema для дедупликации**:
```json
{
  "deduplication_status": "checked|failed",
  "is_duplicate": true|false,
  "duplicate_of_item_id": 123,
  "similarity_score": 85.5,
  "matched_entities": ["Entity1", "Entity2"],
  "matched_events": "Description of matching event",
  "confidence": 0.95
}
```

2. **OutputSchema для иллюстраций**:
```json
{
  "generation_status": "success|failed",
  "image_path": "/path/to/image.png",
  "image_url": "https://...",
  "image_dimensions": {"width": 1024, "height": 1024},
  "prompt_used": "...",
  "error_message": null
}
```

3. **Конфигурация для каждого модуля**:
```json
{
  "summarization": {
    "enabled": true,
    "models": [...],
    "retry_count": 2,
    "timeout": 120,
    "prompt_file": "..."
  },
  "deduplication": {
    "enabled": true,
    "similarity_threshold": 0.85,
    "compare_last_n_days": 7,
    ...
  },
  "translation": {
    "enabled": false,
    "target_languages": ["en", "ru"],
    ...
  },
  "illustration": {
    "enabled": false,
    "image_path": "/path/to/images",
    ...
  }
}
```

---

## 📊 СТАТУС РЕАЛИЗАЦИИ

### ✅ ЗАВЕРШЕНО:
- [x] SummarizationService - production ready
- [x] Схема БД (ai_pipeline_schema.sql)
- [x] VIEW для объединения данных
- [x] PipelineModuleInterface
- [x] Документация

### ⏳ В РАЗРАБОТКЕ:
- [ ] DeduplicationService
- [ ] TranslationService
- [ ] IllustrationService
- [ ] Интеграционные тесты

---

## 🎓 ВЫВОДЫ

**Текущая архитектура ПРАВИЛЬНАЯ и PRODUCTION READY.**

Проблемы в ТЗ были вызваны:
1. Недопониманием принципов нормализации БД
2. Смешением понятий "storage" и "journal"
3. Попыткой создать "единую таблицу для всего"

**Рекомендую:**
- ✅ Использовать существующую архитектуру
- ✅ Доработать OutputSchema для дедупликации и иллюстраций
- ✅ Реализовать недостающие модули по образцу SummarizationService
- ✅ Использовать VIEW для получения полных данных

---

**Документ подготовлен:** AI Assistant  
**Версия:** 1.0  
**Последнее обновление:** 2025-11-08
