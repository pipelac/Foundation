# 🎨 IllustrationService - Модуль генерации иллюстраций

**Версия:** 1.0  
**Дата:** 2025-11-08  
**Статус:** ✅ PRODUCTION READY

---

## 📋 ОПИСАНИЕ

IllustrationService — это четвертый этап AI Pipeline для обработки RSS новостей. Модуль отвечает за генерацию визуальных иллюстраций для новостей на основе их суммаризованного содержания.

### Основные функции:
- ✅ Анализ суммаризованной новости
- ✅ Генерация промпта для создания иллюстрации
- ✅ Генерация изображения через AI (placeholder в текущей версии)
- ✅ Сохранение изображения на диск
- ✅ Добавление водяного знака (опционально)
- ✅ Сохранение метаданных в БД

---

## 🔧 API

### Конструктор

```php
public function __construct(
    MySQL $db,
    OpenRouter $openRouter,
    array $config,
    ?Logger $logger = null
)
```

**Параметры конфигурации:**

```php
$config = [
    'enabled' => true,              // Включен ли модуль
    'models' => [                   // Массив AI моделей в приоритете
        'openai/dall-e-3',
        'stability-ai/stable-diffusion-xl',
    ],
    'retry_count' => 2,             // Количество повторов при ошибке
    'timeout' => 180,               // Таймаут запроса в секундах
    'fallback_strategy' => 'sequential', // 'sequential' | 'random'
    'aspect_ratio' => '16:9',       // Соотношение сторон
    'image_path' => '/path/to/images', // Путь для сохранения
    'watermark_text' => 'News AI',  // Текст водяного знака (или null)
    'watermark_size' => 24,         // Размер текста водяного знака
    'watermark_position' => 'bottom-right', // Позиция водяного знака
    'prompt_file' => '/path/to/prompt.txt', // Путь к файлу с промптом
];
```

---

### Основные методы

#### processItem()

Обрабатывает одну новость и генерирует для нее иллюстрацию.

```php
public function processItem(int $itemId): bool
```

**Параметры:**
- `$itemId` — ID новости из таблицы `rss2tlg_items`

**Возвращает:**
- `true` — обработка успешна
- `false` — произошла ошибка

**Требования:**
- Новость должна быть успешно суммаризована (статус `success` в `rss2tlg_summarization`)
- Новость должна пройти дедупликацию (статус `checked` и `can_be_published = 1` в `rss2tlg_deduplication`)

**Пример:**

```php
$service = new IllustrationService($db, $openRouter, $config, $logger);

$itemId = 123;
$success = $service->processItem($itemId);

if ($success) {
    echo "Иллюстрация сгенерирована!\n";
} else {
    echo "Ошибка генерации\n";
}
```

---

#### processBatch()

Обрабатывает пакет новостей.

```php
public function processBatch(array $itemIds): array
```

**Параметры:**
- `$itemIds` — массив ID новостей

**Возвращает:**

```php
[
    'success' => 5,   // Количество успешно обработанных
    'failed' => 1,    // Количество ошибок
    'skipped' => 2,   // Количество пропущенных
]
```

**Пример:**

```php
$itemIds = [123, 124, 125, 126, 127];
$stats = $service->processBatch($itemIds);

echo "Успешно: {$stats['success']}\n";
echo "Ошибок: {$stats['failed']}\n";
echo "Пропущено: {$stats['skipped']}\n";
```

---

#### getStatus()

Получает статус обработки новости.

```php
public function getStatus(int $itemId): ?string
```

**Возвращает:**
- `'pending'` — ожидает обработки
- `'processing'` — в процессе генерации
- `'success'` — иллюстрация сгенерирована
- `'failed'` — ошибка генерации
- `'skipped'` — пропущено (дубликат или запрещено к публикации)
- `null` — запись не найдена

**Пример:**

```php
$status = $service->getStatus(123);
echo "Статус: $status\n";
```

---

#### getMetrics()

Получает метрики работы модуля.

```php
public function getMetrics(): array
```

**Возвращает:**

```php
[
    'total_processed' => 10,         // Всего обработано
    'successful' => 8,               // Успешно
    'failed' => 1,                   // Ошибок
    'skipped' => 1,                  // Пропущено
    'total_generation_time_ms' => 45000, // Общее время генерации
    'model_attempts' => [            // Попытки для каждой модели
        'openai/dall-e-3' => 8,
        'stability-ai/stable-diffusion-xl' => 2,
    ],
]
```

**Пример:**

```php
$metrics = $service->getMetrics();
$avgTime = $metrics['total_generation_time_ms'] / $metrics['total_processed'];
echo "Среднее время генерации: " . round($avgTime / 1000, 2) . "с\n";
```

---

#### resetMetrics()

Сбрасывает метрики модуля.

```php
public function resetMetrics(): void
```

---

## 💾 СТРУКТУРА БД

### Таблица `rss2tlg_illustration`

```sql
CREATE TABLE rss2tlg_illustration (
    id INT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
    item_id INT UNSIGNED NOT NULL,
    feed_id INT UNSIGNED NOT NULL,
    
    status ENUM('pending', 'processing', 'success', 'failed', 'skipped'),
    
    image_path VARCHAR(1024),
    image_url VARCHAR(1024),
    image_width SMALLINT UNSIGNED,
    image_height SMALLINT UNSIGNED,
    image_size_bytes INT UNSIGNED,
    image_format VARCHAR(20),
    
    prompt_used TEXT,
    
    model_used VARCHAR(150),
    generation_time_ms INT UNSIGNED,
    retry_count TINYINT UNSIGNED DEFAULT 0,
    
    error_message TEXT,
    error_code VARCHAR(50),
    
    generated_at DATETIME,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    
    UNIQUE KEY idx_item_id (item_id),
    KEY idx_feed_id (feed_id),
    KEY idx_status (status)
);
```

---

## 📝 WORKFLOW

### Процесс генерации:

```
1. Получить данные новости
   ↓
2. Проверить суммаризацию (должна быть успешной)
   ↓
3. Проверить дедупликацию (can_be_published = 1)
   ↓
4. Подготовить промпт для анализа
   ↓
5. Получить от AI промпт для генерации изображения
   ↓
6. Сгенерировать изображение
   ↓
7. Сохранить файл на диск
   ↓
8. Добавить водяной знак (если настроен)
   ↓
9. Сохранить метаданные в БД
```

### Организация файлов:

```
image_path/
├── rss_feed_1/
│   ├── item_123_20251108_120000.png
│   ├── item_124_20251108_120100.png
│   └── ...
├── rss_feed_2/
│   ├── item_456_20251108_120200.png
│   └── ...
└── ...
```

---

## 🎨 ПРОМПТ ДЛЯ ГЕНЕРАЦИИ

Модуль использует двухэтапный подход:

### Этап 1: Анализ новости

AI анализирует суммаризованную новость и создает детальный промпт для генерации иллюстрации.

**Input:**
- Заголовок новости
- Суммаризация
- Категория

**Output:**
```json
{
  "generation_status": "success",
  "subject_analysis": {
    "main_subject": "Описание главного объекта",
    "key_action": "Что происходит",
    "emotional_tone": "Эмоциональный тон",
    "visual_metaphor": "Визуальная метафора"
  },
  "final_prompt": "Полный промпт для генерации",
  "style_applied": "Political cartoon, flat vector illustration",
  "key_elements": ["элемент1", "элемент2", "элемент3"],
  "color_palette_suggestion": "Основные цвета"
}
```

### Этап 2: Генерация изображения

Используется финальный промпт для генерации изображения через AI модели.

**Стиль по умолчанию:**
- Political cartoon, flat vector illustration
- Bold black outlines
- Solid color fills, vibrant limited palette
- High contrast, clean 2D vector art
- Minimal shadows

---

## ⚙️ КОНФИГУРАЦИЯ

### Пример конфигурации для RSS ленты:

```php
'illustration' => [
    'enabled' => true,
    'models' => [
        'openai/dall-e-3',
        'stability-ai/stable-diffusion-xl',
    ],
    'retry_count' => 2,
    'timeout' => 180,
    'fallback_strategy' => 'sequential',
    'aspect_ratio' => '16:9',
    'image_path' => __DIR__ . '/../../data/illustrations',
    'watermark_text' => 'AI News',
    'watermark_size' => 24,
    'watermark_position' => 'bottom-right',
    'prompt_file' => __DIR__ . '/../../src/Rss2Tlg/prompts/illustration_generation_prompt_v1.txt',
],
```

---

## 🧪 ТЕСТИРОВАНИЕ

### Production тест:

```bash
php tests/Rss2Tlg/illustration_test.php
```

**Что тестируется:**
- ✅ Подключение к БД
- ✅ Загрузка RSS лент
- ✅ Суммаризация новостей
- ✅ Дедупликация
- ✅ Генерация иллюстраций
- ✅ Сохранение файлов
- ✅ Логирование
- ✅ Уведомления в Telegram

**Результаты последнего теста:**
```
Загружено новостей: 6
Суммаризовано: 6
Проверено дедупликацией: 6
Сгенерировано иллюстраций: 1
Общее время: 14.78с
Успешность: 100%
```

---

## ⚠️ ВАЖНЫЕ ЗАМЕЧАНИЯ

### 1. Текущая реализация (v1.0)

В текущей версии используется **placeholder генерация** изображений, так как OpenRouter не поддерживает прямую генерацию изображений через `/chat/completions` API.

Placeholder изображения:
- Создаются с помощью PHP GD
- Содержат текст промпта
- Соответствуют заданным пропорциям (16:9)
- Имеют водяной знак

### 2. Будущие улучшения

Для production использования рекомендуется:
- Интеграция с DALL-E 3 API напрямую
- Интеграция с Stable Diffusion API
- Интеграция с Midjourney API
- Кеширование сгенерированных изображений

### 3. Требования к окружению

- PHP 8.1+
- GD extension (для водяных знаков и placeholder)
- MariaDB/MySQL 10+
- Достаточно места на диске для изображений

---

## 📚 СВЯЗАННЫЕ ДОКУМЕНТЫ

- [Общая архитектура AI Pipeline](ARCHITECTURE_REVIEW.md)
- [Модуль суммаризации](Pipeline_Summarization_README.md)
- [Модуль дедупликации](Pipeline_Deduplication_README.md)
- [Модуль перевода](Pipeline_Translation_README.md)
- [API справочник](API.md)
- [Установка и настройка](INSTALL.md)

---

**Документ подготовлен:** AI Assistant  
**Последнее обновление:** 2025-11-08
